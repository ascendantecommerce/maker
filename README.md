script-to-video
/quick-edit/{projectId}

ai-copilot
/ai-copilot/{projectId}

ai-editor
/quick-edit/{projectId}

ai-captions
/quick-edit/{projectId}

link-to-video
/quick-edit/{projectId}

ai-editor
/quick-edit/{projectId}

/edit/{schemaId}

- get scene by schema id
  if scene exists, load it

- get schema by id
  schema to scene convert
  create a new scene

scene -> schemaId, id

1. Default font should be THE BOLD FONT
2. REduce default caption size
3. update editor ui. pull from combo-editor or ai-editor
4. all caption presets should have a font family and font url
5. update script to video. caption position, size, music

6. script to video dispatching twice
